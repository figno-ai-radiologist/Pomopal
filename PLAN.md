# PomoPal — Canonical Implementation Plan (v2.1 — APPROVED)

**Product owner:** Fable 5 Medium (planning, review). **Executor:** Sonnet 5 Ultracode (implementation).
**Status:** Approved by the user 2026-07-17 with amendments A1–A6 (incorporated below). Pass 1 authorized. Each later pass begins only after Fable review of the previous pass's evidence.
Sonnet must not edit this file; Fable records status and deviations here.

---

## §0 Live-product audit summary (evidence basis)

Deployed build (= repo `aa49d58`) measured at 320×568, 375×667, 390×844, 768×1024, 1280×720 with seeded realistic data. Key findings driving decisions below:

- **L1** Real bug: `main`'s transform entrance animation makes it the containing block for `position:fixed` `#setDock` → dock renders mid-page ~0.65s on every load, then snaps to bottom.
- **L2** `#setDock` lives inside `#tab-timer` — settings/ambience/music unreachable from other tabs while music keeps playing.
- **L3** 320×568: dock handle overlaps the Start button by ~26px.
- **L5** Task rows balloon: 72px @768 → 164px @390 → 300px @320 (wrapped note).
- **L6** Touch targets: checkbox 22×22 (all widths); goal target-edit span 16×17; Working-on pill 29px tall; chips 35px.
- **L7** Coarse-pointer tablets >520px get only the `.mv` bump; steppers 30px, checkbox 22px.
- **L8** Goals form defaults to **Count** — the only type that never auto-ticks. Default path silently never progresses.
- **L9** "Planner" vs "Goals" labels are both prospective-sounding; placement of e.g. "read 10 chapters" is ambiguous.
- **L10** Passing: no horizontal overflow anywhere 320–1280; subject modal fits at 320; 16px form fonts (no iOS zoom).
- **L11** Every list rebuild replays the `rise` entrance on all rows (checking one task re-animates the whole list).
- **L13** Native task `<select>` sits inside the ring face; phase icons measure adequate → **ring swipe rejected** (§8).

## §1 Locked product decisions

| # | Decision |
|---|----------|
| D1 | **IA: Timer / Plan / History.** Inside Plan: **Today** and **Targets** subsections behind a segmented control (shared pattern with the settings dock's sub-tabs). History keeps its name. Mental model: Targets = direction, Today = current commitment, Timer = the work, History = the record. |
| D2 | **Primary target types are auto-derived:** **Pomodoros** (count of auto-logged focus completions `h.auto === true` with `h.at ≥ createdAt`, optional subject scope) and **Minutes** (existing `unit:"min"` derivation). Both support optional subject + deadline. **Form default = Pomodoros.** |
| D3 | **Legacy manual count goals preserved as "Milestone (manual)"** — data, ticks, and progress untouched; rows get a `· milestone` meta tag; creation stays available as the last, non-default type option. No silent deletion or reinterpretation. |
| D4 | **Labels-only rename; storage unchanged.** Keys `study.plan`/`study.goals`/`study.tab`, `data-tab="plan"/"hist"`, section ids, function names all stay. New key: `study.planSub`. |
| D5 | **Runtime tab fallback (permanent):** stored `"goals"` → show `plan` + set `planSub="targets"` (persisted); any unmatched tab → `"timer"`. `study.planSub` validated everywhere it is read: `today`\|`targets`, unknown → `today`. **A5:** `study.planSub` is added to `DATA_KEYS` (exported/imported; imported values validated on restore). |
| D6 | **Swipe right on a Today row = complete.** Incomplete rows only; done rows give no right-swipe response. Un-completing stays on the checkbox. |
| D7 | **Swipe left = reveal actions, never destroy.** Tray shows **Edit note** and **Delete** (≥64px each). **A4:** the label is exactly "Edit note" (it opens the existing inline note editor); no full task-edit modal in this effort. Delete routes through the existing delete+undo. One tray open at a time; outside tap / scroll / right-swipe closes it. |
| D8 | Gesture engineering: pointer events, `pointerType !== "mouse"`, 10px axis lock, `setPointerCapture`, `touch-action: pan-y`, capture-phase click suppression after movement, delegated handlers on `#planList`, per-task-id state, mid-gesture re-render cancels, excluded starts on `input`/`.mv`/`.x`/active-contentEditable. Every gesture keeps a visible button equivalent. |
| D9 | **Haptics: semantic `haptic(kind)` only** (§4). Toggle rendered only when `'vibrate' in navigator`; independent of Sound; default ON. |
| D10 | **Ring phase-swipe: rejected** (§8). Pass 4 is timer motion only. |
| D11 | **Dock:** `#setDock` moves out of `<main>` to document level (fixes L1) and becomes global (fixes L2, deliberate). **A3:** Timer, Plan, and History all get bottom clearance ≥ handle height + `env(safe-area-inset-bottom)` so the handle never covers final content; **the dock closes automatically on top-level tab navigation** (open state does not persist across tabs). |
| D12 | **List motion (A2, corrected):** existing rows render immediately, always — including on initial page load and after backup import. Only the row created by the **current user action** gets the entrance animation, driven by explicit one-shot creation state (`freshId.plan/goal/hist`), set **only** in the genuine creation handlers (`planForm`/`goalForm`/`histForm` submit). Freshness is never inferred from whether an ID has rendered before. The next render applies `.fresh` to the matching row only, clears the one-shot ID on consumption, and removes the class on `animationend` with a 400ms timeout backstop (reduced-motion / hidden-container safe). Auto-logged history rows and undo-restored rows are NOT marked fresh (Fable decision: they are not the current user's creation gesture on that list). Task-completion visual state uses the existing `done` class and never `.fresh`. Returning to Plan, switching Today/Targets, and post-completion rebuilds replay nothing. Sub-tab switching is instant in Pass 1; a restrained container fade (never per-item replay) may be added in Pass 5. |
| D13 | Unchanged: motion tokens (§3), no new dependencies, no visual redesign, old-backup compatibility, one pass per commit, Fable review after every pass. |

## §2 Interaction specifications

**2.1 Today rows.** Base per D8. Right-swipe: mint check beneath, follow-finger; commit at 80px or 48px with release velocity >0.5 px/ms; **haptic: one `success` fired at the moment the threshold is first crossed, latched — nothing on release or commit (A1)**; sub-threshold release snaps back over `var(--dur-reveal)`. Left-swipe: tray with **Edit note** | **Delete**; snaps open at tray width; **haptic: one `selection` when the tray locks open (A1)**; tray buttons are ordinary focusable buttons. No interaction may stack `selection` + `success` (A1); the utility's coalescing window is the backstop.

**2.2 Row layout (A6).** Normal rows compact; long notes clamp to **two lines at rest** (full text reachable via Edit note); no controls hidden; no horizontal overflow at 320px; visible touch controls comfortably tappable. (Implemented in Pass 3.)

**2.3 Targets form (A6).** No fixed pixel-height target. Criteria: no horizontal overflow; logical field grouping; readable labels; primary controls ≥44px tall where practical; no unnecessary single-field rows; the form must not visually dominate the initial mobile viewport; no information or functionality removed merely to reduce height. (Implemented in Pass 5.)

**2.4 Not in scope:** ring swipe, long-press reorder, swipes on Targets/History rows, tab-swipe, pull-to-refresh, task-edit modal.

## §3 Motion tokens & motion-quality fixes

Tokens: `--dur-fast .15s` (absorbs .15/.18/.2), `--dur-reveal .25s` (absorbs .25/.28/.3), `--dur-slide .35s`, `--dur-entrance .45s`, `--dur-celebrate .8s` (absorbs .8/.9), `--ease-spring cubic-bezier(.3,1.4,.4,1)` (absorbs the .3,1.3 variant). Deliberate literals: `breathe 4s ease-in-out`, ring `linear`. `rise` unifies on `ease-out`. Cheer's `setTimeout(900)` → `animationend` + 1s reduced-motion safety fallback. Plus: D11 dock relocation (Pass 2) and D12 fresh-row mechanism (Pass 1).

## §4 Haptic specification

```js
const HAPTIC = { selection: 10, success: 20, sessionComplete: [30, 50, 30] };
const PRIORITY = { selection: 0, success: 1, sessionComplete: 2 };
let lastHaptic = { t: 0, p: -1 };
function haptic(kind) {
  try {
    if (!settings.haptics || !("vibrate" in navigator)) return;
    if (document.visibilityState !== "visible") return;
    const now = performance.now();
    if (now - lastHaptic.t < 250 && PRIORITY[kind] <= lastHaptic.p) return;
    lastHaptic = { t: now, p: PRIORITY[kind] };
    navigator.vibrate(HAPTIC[kind]);
  } catch {}
}
```

**Complete site list (Pass 6 final — supersedes A1's threshold timing):** *a haptic fires only when the associated state change becomes real and committed.* (1) **Manual task completion → one `success`**, placed in the single shared completion path (the checkbox `onchange`, gated on checking) — covers both the visible checkbox and a committed right-swipe (which routes through that same path); cancelled/retreated swipes, done-row swipes, unchecking, and `autoLog()` auto-completion fire zero. (2) **Left tray locks open → one `selection`** at the release-time lock, never at intermediate drag distances; partial swipes, scroll conversions, and tray-action taps fire zero; repeated threshold play within one gesture ≤ one. (3) **Live focus completion → one `sessionComplete`** beside the existing live feedback (never inside `chime()`), gated to focus phases — breaks, `live=false` restores, manual phase switches, reset/start/pause fire zero. **No other sites:** navigation, sub-switching, undo, delete, Edit note, milestone ticks, target completion, phase icons, settings, music, seek, reorder, drag, form submission, start/pause. The two stale Pass 3 hook comments are updated/removed to match. Raw patterns never appear at call sites. Settings row absent when `'vibrate' in navigator` is false.

## §5 Accessibility & reduced-motion

Targets (mostly Pass 7): checkbox, goal target-edit, Working-on pill hit areas ≥40px (visuals may stay compact — pseudo-element halos / padded invisible controls); tour dots ≥24px; tap-target bumps duplicated into `(pointer:coarse)` (L7). Plan sub-tabs: `role="tablist"`/`aria-selected` mirroring the dock pattern; subsections keyboard-reachable. Reduced-motion blanket kill stays; swipe transforms are direct style writes; tray reveal instant under reduced motion; haptics independent of reduced-motion.

## §6 Data migration

No key renames or data rewrites. `unit:"poms"` added (new rows only); `goalProgress` extended; legacy `count` rows untouched. `study.planSub` added + validated + in `DATA_KEYS` (A5). `study.tab` runtime map (D5). `settings.haptics` joins defaults (Pass 2). Old 4-tab backups: import → reload → `"goals"` lands on Plan/Targets.

## §7 Passes

**Evidence rules:** §9 statuses move to "in progress" only when Sonnet 5 Ultracode actually begins the pass. Where an acceptance item requires a physical touch or vibration-capable device, Sonnet must not claim success without one — it provides emulated/instrumented evidence, clearly labeled, and records physical-device verification as **pending**.

### Pass 1 — Plan IA, Targets model, compatibility migration ← **AUTHORIZED**

**Files:** `index.html`, `README.md` only. PLAN.md is Fable-owned.

**Scope (complete; nothing else):**
1. Nav: 3 buttons — Timer / **Plan** / History (`data-tab` values `timer`/`plan`/`hist` unchanged); Goals button removed; `#navPill` width → the 1/3 pattern already used by `.setPill`.
2. Plan section: segmented control (Today | Targets) above the content, reusing the dock sub-tab pattern. If the existing styles are id-bound to `#setTabs`, generalize to a shared class used by both containers (each pill positioned by a custom property set on its own container); the settings dock's behavior must remain pixel-identical. Subsection containers: Today = existing planner form+list block (h2 "Today's plan" and hint unchanged); Targets = the goals hint/form/list block moved in from `#tab-goals` (which is removed). `showPlanSub(name, persist=true)` mirrors `showSetTab`: aria-selected, pill var, hidden toggling, `save("study.planSub", ...)` when `persist`.
3. Targets vocabulary: h2 "Your **targets**"; hint: "Bigger than today. Pomodoro and minute targets fill up on their own as you focus; milestones tick by hand."; label "Goal"→"Target"; text placeholder → "A steady study week"; empty state `emptyState("No targets yet — aim at something.")` (mascot OK — only one list visible per view now); delete toast "Target deleted".
4. Targets model: `#goalUnit` options in order — `poms` "Pomodoros" (first = default), `min` "Minutes", `count` "Milestone (manual)". `goalProgress` for `poms`: `hist.filter(h => h.auto && h.at >= (g.createdAt||0) && (!g.subjectId || h.subjectId === g.subjectId)).length`. Subject select (`#goalSubjF`) visible when unit ≠ `count`; submit stores `subjectId` for `poms` and `min`. Meta for poms: `cur + " / " + target + " poms"`; poms/min never show "+N over" (cap bar at 100%, "· done ✦" as today); count keeps existing over-display; count rows additionally get `· milestone` in meta. Target default value 10 stays.
5. Tab fallback (D5/A5): in `showTab`, raw `"goals"` → set `planSub="targets"` (persisted) and show `plan`; unmatched → `"timer"`. `study.planSub` read via validated load (`today`|`targets` else `today`), written by `showPlanSub`, added to `DATA_KEYS`.
6. Fresh-row mechanism (D12, corrected — applies to `#planList`, `#goalList`, `#histList`): explicit one-shot creation state `freshId = { plan, goal, hist }`, set only in the three creation submit handlers immediately before their render call. During render, the row whose id matches consumes the one-shot (gets `.fresh`, id cleared); `.fresh` is removed on `animationend` (delegated per list) with a 400ms timeout backstop. Persisted rows do not animate on initial load; imported rows do not animate; auto-logged and undo-restored rows do not animate; tab return / sub-switch / rebuild replay nothing.
7. Tour: step 4 `{tab:"plan", sub:"today", sel:"#planForm", h:"Today", ...}`; step 5 `{tab:"plan", sub:"targets", sel:"#goalForm", h:"Targets", p:"Bigger than today. Pomodoro and minute targets fill up on their own; milestones tick by hand."}`; `tourShow` honors an optional `sub` via `showPlanSub(sub, false)` (**no persistence from the tour**); `tourEnd` restores `showPlanSub(load-validated planSub, false)` alongside the saved tab. Step copy for the old planner/goals steps rewritten to Today/Targets vocabulary; History step unchanged.
8. README: Planner section → "Plan" describing Today + Targets subsections; Goals section content folds in (auto pomodoro/minute targets, milestones tick by hand); "subjects shared across planner, log, and goals" → updated wording; nav description updated. History wording untouched.
9. Strings sweep: every "Planner"/"plan"/"Goals"/"goal" user-visible string from the rename-surface audit addressed with Plan/Today/Targets vocabulary. Internal identifiers untouched (D4).

**Explicitly out of scope for Pass 1:** dock move/clearance/auto-close (Pass 2), motion tokens (Pass 2), `haptic()` (Pass 2), swipes (Pass 3), note clamping (Pass 3), form layout work (Pass 5), any haptic call site, ring anything.

**Edge cases that must work:** `timerTaskId`/`renderTaskSel` coupling unchanged; goals created pre-change (min + count) render and compute correctly; `study.tab === "plan"` users land on Today (default sub); import of a pre-restructure backup (incl. `study.tab:"goals"`, no `study.planSub`) lands on Plan/Targets after reload; re-running the tour from `?` doesn't corrupt the persisted sub-state; `console.assert` self-checks still pass.

**Acceptance criteria:**
- A. Three tabs; pill 1/3 width and correct position on each; no dead nav states.
- B. Persisted rows never animate on initial load; imported rows never animate as newly created; Today|Targets switch (instant, persisted) and returning to Plan replay zero entrances; rebuilding a list after a completion replays zero.
- C. Only the row created by the current user action (task add / target add / manual log) receives the entrance, exactly once; auto-logged and undo-restored rows do not; task-completion state uses the existing `done` class and never `.fresh`.
- D. Stored `"goals"` (boot, import-reload, tour-end) lands Plan/Targets; unknown tab → Timer; `plan`/`hist`/`timer` unaffected.
- E. `study.planSub` exports/imports, validates (garbage → `today`).
- F. New Pomodoros target auto-progresses after a live focus completion; Minutes target unchanged; legacy count row keeps value, ticks, `· milestone` tag; subject select hidden only for Milestone.
- G. Full tour runs, Targets step lands correctly, sub-state not polluted.
- H. Old-format backup imports cleanly.
- I. README accurate.
- J. Zero console errors; zero horizontal overflow at 320/375/390/768/1280; timer/music/history smoke-pass.

### Pass 2 — Shared motion, interaction, haptic foundations
Tokens + call-site migration; dock relocation out of `<main>`, global availability, **auto-close on top-level tab switch, bottom clearance on all three tabs (A3)**; `haptic()` + conditional Vibration toggle (no call sites); cheer `animationend`; gesture CSS groundwork. *Accepted when:* no dock jump on load; dock reachable from all tabs and closes on tab switch; Start clear of handle at 320×568; final content on every tab clears the handle; tokens verified via computed styles; toggle absent without vibrate support; visuals otherwise unchanged.

### Pass 3 — Today task touch & gesture polish (user-authorized scope, binding)

**Implement only:** (1) swipe right to complete an incomplete Today task; (2) swipe left to reveal an **Edit note** | **Delete** tray; (3) two-line resting clamp for long notes; (4) row-resident touch-target improvements; (5) gesture lifecycle & conflict handling; (6) **structural haptic hook points only** — comment-marked, zero `haptic()` calls until Pass 6.
**Prohibited:** full-swipe deletion, swipe-to-uncomplete, long-press reorder, timer gestures, tab gestures, new dependencies, a task-edit modal, haptic calls, unrelated layout/visual redesign.

**Right swipe:** incomplete tasks only (done rows give zero swipe response; un-completing stays on the checkbox); mint completion affordance beneath; row follows pointer after horizontal intent is claimed; commit at 80px, or 48px with release velocity >0.5 px/ms; completes once; the threshold-crossing latch point is comment-marked for Pass 6 `success`.
**Left swipe:** tray with **Edit note** and **Delete** (each ≥64px, real buttons, focusable only while open); release beyond threshold leaves it open; never deletes from the swipe alone; Delete uses the existing delete+undo path; Edit note enters the existing inline editor and exposes the complete unclamped text; tray-lock point comment-marked for Pass 6 `selection`; one tray open at a time.
**Tray closes when:** outside tap; vertical scrolling begins; another row opens; the row is right-swiped; the list re-renders; the task is deleted or completed; the Plan subsection or top-level tab changes.

**Engineering (all D8 rules retained):** delegated Pointer Events on persistent `#planList`; touch/coarse-pointer only, desktop mouse drag-reorder unchanged; 10px directional-intent threshold with horizontal dominance before claiming; `setPointerCapture` only after claiming; `touch-action: pan-y` on rows; capture-phase suppression of the post-gesture synthetic click; excluded starts on checkbox/inputs, move buttons, delete control, tray buttons, actively editable content; safe cancel on row detach or `renderPlan()` mid-gesture — no leftover transforms, capture, open trays, or state; never intercept ordinary vertical scroll; tolerate iOS edge-navigation precedence near viewport edges.

**Row layout:** notes clamp to two lines at rest (full note stays in DOM and in the editing flow; stored data untouched); subject, pomodoro progress, checkbox, move controls, and delete fallback all stay visible; no horizontal overflow at 320px; compactness improved without readability loss; checkbox and action hit areas comfortably tappable while preserving current visual size where possible; completed styling stays clear without relying on color alone (strikethrough retained).

**Verification matrix (real interactions, not only direct function calls):** rows — short note, long clamped note, no note, completed; positions — first/middle/last; lists — single-row and long-scroll; widths — 320/375/390/768/1280; inputs — mouse, keyboard, touch emulation; modes — reduced motion, light+dark. **Required scenarios 1–20:** sub-threshold right swipe returns cleanly; fast short swipe commits only per the velocity rule; slow <80px swipe doesn't commit; completed row ignores right swipe; left swipe opens tray without deleting; tray buttons tappable and keyboard-focusable; Edit note exposes full text and commits; Delete+Undo restores at the correct position; vertical scroll never opens/completes rows; checkbox tap starts no swipe; move arrows functional; desktop HTML drag-reorder unchanged; inline editing has no gesture/text-selection conflicts; mid-gesture render cancels cleanly; timer auto-completion during a gesture leaves no stale transform or exception; tab/subsection change closes trays; Pass 1 new-row entrance behavior unchanged; zero `haptic(` call sites; zero console errors; zero horizontal overflow. Emulated coarse-pointer evidence must be labeled as such; physical-device verification recorded pending per §7 evidence rules.

### Pass 4 — Timer motion (user-authorized scope, binding)

Narrow timer-quality pass: verify/refine existing motion under the tokens — no redesign, no new gestures. **Prohibited:** ring swipe/long-press/drag, per-second or per-tick digit animation, new continuous ambient animation, confetti/celebration overlays, new sounds, haptic call sites, new dependencies, unrelated Timer layout changes, intensified glow, changes to the global reduced-motion policy.

**Requirements:** start/pause keep immediate state changes with only the existing restrained feedback (pause quieter than start; no large pulse, page transition, or delayed state). Countdown digits visually stable — nothing animates per second/tick/progress-update; the ring keeps its approved linear transition. Phase changes: label, color, ring styling all update correctly through the approved token bucket, no stale focus/break class, no wrong-accent flash, never blocking state updates or input. **Session completion invariants:** completion state and cheer class apply once; cheer clears via `animationend` with a 1s safety fallback for reduced-motion; the fallback can never remove a later, unrelated cheer; repeated completions cannot accumulate listeners or timeouts; `live=false` restores never play the cheer; no haptic calls. (User preference: a `{once:true}` per-cheer listener with explicit safety-timer cleanup; an equivalent single persistent listener that also clears the pending safety timer on `animationend` satisfies the same invariants — implementer documents the choice.) **Breathe:** the 4s glow stays the only continuous timer animation — active only while running, stops immediately on pause/reset/complete, gone after phase changes, disabled under reduced motion, no meaningful paint-cost increase. **Reduced motion:** breathe and cheer disabled, digits stable, phase changes instant, ring progress still correct, cheer class cleared by the fallback, no stale classes, no JS-driven scale/translation introduced. **Dock regression:** re-verify under the original failure condition (main mid-entrance-transform) — pinned, no snap, Timer content clear of the handle, open/close doesn't disturb timer state, top-level nav closes it, music playback survives dock close. **Ring:** zero pointer/touch/swipe/drag handlers, no `touch-action` override, Working-on select opens normally, phase icons remain the only phase-switching control, short-viewport scroll normal.

**Verification scenarios 1–16:** start; pause+resume; reset-while-running; live focus completion; live break completion; phase-icon switch while stopped; switch-while-running with confirm cancelled; with confirm accepted; `live=false` background restore; two completion cycles without duplicated listeners/stale classes; reduced motion; light+dark; 320×568/375×667/390×844/tablet/desktop; dock open/close during stopped and running; Working-on select under touch emulation; console + timer-state inspection throughout. Acceptance is the user's full criteria list, including: timer accuracy, persistence, task attribution, and auto-logging unchanged; no unrelated files or behavior change.

### Pass 5 — Targets inside Plan: layout & interaction polish (user-authorized scope, binding)

Data model untouched (Pass 1 stands). **Prohibited:** new target types, recurring targets, templates, recommendations/AI, charts, drag reorder or swipes on Targets, celebrations beyond the pulse, haptic call sites, new dependencies, History changes, Today-gesture changes, broad redesign, dashboard aesthetic.

**Hierarchy:** Today = what I work on now; Targets = what I'm working toward. Targets must not read as a second task list — differentiated by progress bars, current/target values, subject+deadline metadata, milestone labels; no checkbox, no reorder, no swipe.

**Locked copy:** heading "Your targets"; hint "Pomodoro and minute targets update as you focus. Manual milestones move when you update them."; empty state "No targets yet. Add one to track progress over time." (existing restrained treatment, no new illustration); tour Targets step copy updates to match the new hint's meaning (keep it short). Vocabulary everywhere: Target / Pomodoros / Minutes / Milestone (manual) / Subject / Deadline / Amount — never "goal" in visible UI; internal identifiers unchanged (D4).

**Form (order locked):** 1 Target description (full row on mobile) → 2 Type → 3 Amount (Type+Amount may share a row only if both stay readable and tappable) → 4 Subject → 5 Deadline (share a row when space allows, stack at narrow widths) → 6 **Add target** button (obvious, reachable). No horizontal scroll, no clipped select text, no control under practical touch height to save space. Desktop/tablet: efficient width, no one long row, labels clearly associated, form never visually heavier than the list. Dynamic: Pomodoros/Minutes show Subject, Milestone hides it; hidden fields out of the focus order; type switches cause no page jump and preserve entered values; default stays Pomodoros. After a successful add, focus returns to the description field.

**Rows:** automatic targets read "6 / 20 Pomodoros" / "145 / 300 minutes" (full words), no +/− controls; milestones keep +/− (secondary to description+progress) with a restrained "Milestone" label and must not resemble a Today task. Completed: bar caps at 100%, "done ✦" stays, completion legible without color alone. Deadlines: date + days-remaining when useful; overdue must carry a non-color signal (the word "overdue" stays); no noisy countdown language.

**Pulse (verify exhaustively):** fires only on the incomplete→complete transition; never replays on re-render, subsection switch, Plan return, reload, or text/amount edits that don't genuinely create a new transition (amount edits that DO create one may pulse); same freshness rule for automatic and manual progress; reduced motion disables the decoration while completion state stays visible; no stale pulse class.

**Subsection transition (Fable decision: implement):** one CSS-only container reveal on `.planPanel` reusing the existing `rise` keyframe at `var(--dur-reveal)` ease-out — ≤8px translation + opacity, container-level only, replays on each un-hide by native CSS animation restart, no per-row stagger, no `.fresh` interaction, no interaction delay, killed by the reduced-motion blanket. If it produces layout instability, leave the switch instant and record why.

**Accessibility (do now, not Pass 7):** tablist behavior from `9ef213f` preserved; all form controls labeled; progress bars gain `role="progressbar"` + `aria-valuenow`/`aria-valuemax` + an `aria-label` carrying the target description; milestone +/− get specific accessible names including the target description (not bare +/−); completed/overdue conveyed to AT via existing text; delete keyboard-operable; **inline text/amount editing keyboard-operable — if the shared `editableText` spans are not keyboard-reachable, fix at the component level (tabindex 0 + Enter opens editing) and record as a Fable-sanctioned component fix**; no focus trap or focus loss after add/delete/type switch.

**Verification:** content matrix (none/one/many targets, very long description, Pomodoros, Minutes, subject-scoped, no subject, deadline, overdue, completed, milestone at zero, milestone past amount) × widths (320/375/390/768/1280) × themes (light/dark) × motion (reduced/normal). Regression list 1–16 from the user's authorization (poms derivation incl. `createdAt` cutoff and subject scoping, min calc, legacy milestones, CRUD+Undo, one-time pulse, Today gestures unchanged, timer task selection unchanged, History unchanged, old backups import, `study.planSub`, tour Targets step, zero haptic sites, no overflow, no console errors). Acceptance is the user's full criteria list.

### Pass 6 — Restrained haptic call sites (user-authorized scope, binding)

Exactly the §4 final site list — three call sites, outcome-committed timing. Utility unchanged unless verification reveals a genuine defect (guards: `settings.haptics`, feature-detect, `visibilityState === "visible"`, central patterns, fail-silent, priority coalescing, independent of Sound and reduced-motion). **Capability reverify:** toggle only when supported, no dead control, OFF silences everything, Sound-off doesn't suppress vibration, restored settings preserve the choice, legacy settings without `haptics` migrate cleanly.

**Instrumented verification (physical Android unavailable):** stub `navigator.vibrate` recording call count, semantic event, resolved pattern, timing, visibility state, setting, interaction source — never reported as a physical pass. If the pane reports `visibilityState === "hidden"`, it may be temporarily instrumented to `"visible"` for the scenario runs (labeled), with the real hidden-guard verified separately as scenario 18. **Required call-count scenarios 1–20** per the user's authorization (checkbox ±, committed/cancelled/retreated/done-row right swipes, partial/locked/replayed left swipes, tray-action tap, live focus/short/long completions, `live=false` restore, auto task completion, target completion, OFF, hidden, API-absent no-throw, muted-sound-still-fires). **Stacking test:** one live focus completion that simultaneously auto-completes a Today task and completes a Target → exactly one `sessionComplete`, zero `success`, zero `selection`, no delayed second vibration. **Regressions:** completion state, swipe thresholds/cancellation, tray behavior, timer accuracy/restore, auto-logging, target progress, sound, notifications, reduced-motion, keyboard, desktop drag, unsupported-browser behavior all unchanged; no unrelated CSS/layout/copy/data changes.

### Pass 7 — Accessibility & reduced-motion hardening
§5 items; keyboard walkthrough; reduced-motion sweep over Passes 1–6. *Accepted when:* all targets measure (≥40px, dots ≥24px); coarse-pointer 768 emulation gets bumps; keyboard parity with every gesture; zero decorative motion under reduced motion.

### Pass 8 — Cross-browser & migration QA
Five viewports light+dark; old-backup import; complete tour; timer regression set (reload-mid-session attribution, undo queue, duration clamp); music smoke; iOS sanity; README final. *Accepted when:* all pass with recorded evidence and Fable signs off.

## §8 Deviations & audit-driven decisions log (append-only)

- Ring phase-swipe **rejected** for this effort (L6 icons adequate, L13 select conflict, zero discoverability). Revisit only with real-device evidence.
- Dock globalization is a deliberate product change (L1 bug fix + L2), with A3 auto-close-on-navigation.
- Milestone creation retained but demoted to last, non-default option (L8, plausible non-activity job).
- A1–A6 amendments incorporated 2026-07-17 (haptic de-duplication; new-row-only entrances; dock clearance + auto-close; "Edit note" label; `study.planSub` in DATA_KEYS with validation; qualitative layout criteria replacing pixel-height targets).
- **Pass 1 corrections (Fable-decided during review):** (1) `trackFresh` gained a 400ms `setTimeout` backstop removing `.fresh` — `animationend` cannot fire on rows hidden mid-animation or under reduced motion, and a stale `.fresh` would replay the entrance on re-show (verifier-caught race against acceptance B). (2) The Targets form's numeric field relabeled "Target" → **"Amount"** — the plan's literal string mapping had produced two adjacent "Target" labels (implementer-flagged).
- **Pass 1 verification notes:** the boot-time re-save of migrated arrays (pre-existing, idempotent) is accepted as-is; ARIA tablist roles on the Plan segmented control are deferred to Pass 7 as planned; empty-state rows no longer animate (consistent with D12 — only new rows animate).
- **Pass 1 follow-up (user-directed, narrow — NOT Pass 7 scope expansion):** full ARIA tablist semantics + keyboard interaction for the Today/Targets segmented control only (`#planSub`): `role="tablist"` + accessible label on the container; `role="tab"`, stable ids, `aria-controls`, correct `aria-selected`, roving `tabindex` (0 selected / −1 otherwise) on the buttons; `role="tabpanel"` + `aria-labelledby` on the panels; Left/Right arrows with wrap, Home→Today, End→Targets; keyboard selection persists `study.planSub` and moves focus; pointer activation unchanged; tour-driven changes update ARIA/tabindex but never persist; invalid restored/imported sub-state still resolves to Today. No visual change. The settings dock's `#setTabs` stays untouched (Pass 7). Committed separately on top of `b12e176`.
- **Pass 6 product ruling (Fable, from the scenario-5 conflict):** Pass 3's one-way commit latch contradicted the user's "a right swipe remains cancellable until release." Ruling: commit is decided at release only — final |dx| ≥ 80px, or ≥ 48px with directional release velocity — both directions symmetric; the `latched` flag removed entirely. §2.1's "commit at 80px" now means release-position, not crossing. The Pass 6 implementer correctly refused to change this unilaterally and escalated; the correction was applied and re-verified (cross-retreat-release cancels with zero haptics; plain/velocity commits and tray lock unchanged; done-row rejection and scroll-claim behavior intact).
- **Pass 6 timing correction (user-directed):** A1's "fire `success` at threshold crossing, latched" is superseded — a swipe past 80px remains cancellable until release, so threshold-time firing gives false confirmation. New rule: haptics confirm committed outcomes only. §4 site list rewritten accordingly; the Pass 3 threshold-latch hook comment is retired in favor of the shared completion path.
- **Pass 5 defect (verifier-caught, Fable-corrected):** `.pulse` was never removed after being added — a target completed while the Targets panel was hidden (autoLog rendering off-screen) replayed its pulse on every subsequent subsection reveal indefinitely. Fixed with the `.fresh` pattern: delegated `barPulse` `animationend` removal + a 1s per-node timeout backstop; a strict no-replay reading was chosen over a deferred first-play (completion state stays visible via `full` + "done ✦").
- **Pass 5 ratifications (Fable):** (1) `#goalSubjF[hidden]{display:flex; visibility:hidden}` — fixes a genuine pre-existing Pass 1 defect (the two-class `.f` rule outranked the UA `[hidden]` rule, so the Subject field was never actually hidden or removed from focus order) and simultaneously satisfies this pass's no-jump requirement by reserving layout space; (2) the sanctioned `editableText` component fix (tabIndex 0 + Enter-to-open) also grants Today notes and History notes keyboard entry — accepted as a uniform a11y improvement; (3) `aria-valuemin="0"` kept (correct ARIA, free); (4) the "zero haptic call sites" invariant holds — a Pass 3 prose comment merely contains the substring.
- **Pass 3 inline ruling (Fable):** the left-swipe tray remains available on completed rows — Delete is a legitimate action there; only rightward (completion) movement is blocked on done rows. "No swipe behavior on already completed tasks" is read as governing the completion gesture, not row deletion access.
- **Pass 2 correction (user-directed):** `touch-action:pan-y` removed from `#ring` — ring gestures are rejected (D10) and the ring hosts the native Working-on select; groundwork stays only on the Pass 3 row surfaces. Pass 1 follow-up + Pass 2 approved by the user; the `f719553` legacy-tab consumption fix confirmed correct.
- **Pass 1 migration defect (found during follow-up verification, Fable-corrected):** the `showTab` goals-migration branch never consumed the stored legacy value, so the remap re-fired on every boot and at tour end, re-persisting `planSub="targets"` and clobbering a legacy user's later Today choice. Fix: the branch now also persists `study.tab = "plan"` — consumed once, while remaining permanent runtime defense against re-imported legacy backups.
- **D12 contradiction correction (user-directed, blocking):** the initially shipped seen-ID freshness inference animated ALL rows on first paint (persisted and imported rows included), violating D12's "existing rows always render immediately." Replaced with explicit one-shot creation state (`freshId.plan/goal/hist`) set only in the three creation submit handlers; auto-logged and undo-restored rows deliberately not marked (Fable decision). Acceptance B/C rewritten to match. Metadata corrections applied in the same revision: planning model named "Fable 5 Medium"; §9 status semantics ("in progress" only when Sonnet actually begins); physical-device evidence rule added to §7.

## §9 Completion status

| Pass | Status |
|---|---|
| 1 — Plan IA, Targets model, migration | **complete** — implemented by Sonnet (`de0fd8d`), then the user-flagged D12 contradiction corrected by Sonnet (explicit one-shot `freshId`, this commit); verified by independent reviewers + Fable live review both times; acceptance A–J pass under the corrected B/C. Physical-device verification pending (per §7 evidence rules): visual confirmation of the entrance animation on a real painting foreground tab, and reduced-motion behavior on a real device. |
| 2 — Foundations | **complete** — motion tokens migrated file-wide (verifier: zero missed literals; deliberate literals intact); dock relocated to body level (L1 structurally fixed — verified pinned while `main` held a frozen transform), global + auto-close on tab switch (L2/A3), clearance formula on all tabs (L3: 25.7px overlap → ~0–6px clearance at 320×568, thin margin flagged for Pass 8 device check); `haptic()` per §4 with zero call sites; conditional Vibration toggle; cheer `animationend` + 1s fallback; `touch-action` groundwork. Fable-ratified deviations: ≤520px vertical-rhythm trims (only possible L3 fix), numeric-bucket token migration, ring glow → `--dur-celebrate`. Physical-device pending: real vibration, real animation playback, toggle absence on iOS, real touch scroll over `pan-y` surfaces. |
| 3 — Today swipes | **complete** — gesture engine (delegated pointer events, single-gesture state, 10px axis-locked claim, capture-after-claim, done-row rightward rejection, 80px latch / 48px+velocity commit through the checkbox's own path, tray with Edit note + Delete via the existing undo path, all seven close rules wired, capture-phase one-shot click suppression), two-line resting note clamp lifted while editing, 40px checkbox hit label, ≤520px row trims (long-note row @320px: ~300px → 188px measured). All 20 matrix scenarios PASS (emulated PointerEvents, labeled); both verifiers PASS on every criterion; Fable live review re-confirmed commit/rejection/tray/undo/clamp end-to-end. Fable-ratified deviations: dragOver indicator offset (forced by overflow:hidden), 140px travel cap (`ponytail:`-marked), dark-theme tray/mint contrast fix. Zero `haptic()` calls — two comment-marked Pass 6 hooks. Physical-device pending: all 20 scenarios on real touch hardware, OS-level reduced-motion, iOS edge-navigation precedence, real pointer-capture semantics. |
| 4 — Timer motion | **complete** — verification-dominated pass; sole code change is the 2-line cheer hardening (`animationend` now also clears the pending 1s fallback, closing the stale-fallback-strips-a-later-cheer race; single persistent listener shape chosen over `{once:true}` with documented rationale — no accumulation possible by construction, proven via an instrumented double-completion race test). All 16 scenarios PASS: start/pause/reset feedback unchanged, no digit animation, both cheer-clear paths, `live=false` restore plays no cheer, breathe strictly running-only, reduced-motion stand-in clean, dock regression re-proven (endAt untouched across open/close while running; music iframe node identity survives dock close), ring has zero gesture code and no `touch-action`, attribution/auto-log fundamentals byte-identical. Verifier PASS on all 8 criteria; Fable re-confirmed the diff and a live cheer cycle. 320×568 clearance still thin (~6px) — unchanged, remains earmarked for Pass 8. Physical-device pending: real animation playback, OS-level reduced motion, native select picker under real touch, small-screen clearance. |
| 5 — Targets polish | **complete** — locked copy in (hint/empty-state/Add target/tour); form reordered description→Type→Amount→Subject→Deadline with `.fpair` responsive pairing (no media query, verified 320↔1280); type switches jump-free via the space-reserving `[hidden]` fix; rows read "X / Y Pomodoros"/"X / Y minutes"/"· Milestone"; progressbar ARIA + descriptive stepper names; `editableText` keyboard-operable component-wide; one container-level `.planPanel` rise on subsection switch; focus-to-description after add. Full content matrix × 5 widths × themes verified; 16 regressions pass. Verifier-caught pulse-replay defect fixed (animationend + 1s backstop) and re-verified on the exact repro path. Physical-device pending: AT read-through of the new progressbar/keyboard flows, real reduced-motion toggle, tap comfort. |
| 6 — Haptics | **complete** — exactly three call sites, outcome-committed: checkbox `onchange` gated on checking (single shared path covering committed right-swipes — structurally double-fire-proof), tray release-time lock, `finishPhase` live-focus with `wasFocus` captured pre-`nextPhase`. Utility untouched. 20-scenario instrumented battery (dual recorders: semantic log + vibrate stub) all pass after the Fable gesture ruling; stacking test through the real tick path → exactly one `sessionComplete`, none delayed; capability checks pass incl. legacy-settings migration. Scenario 5 exposed the latch conflict → Fable ruling applied (release-time commit, `latched` removed) and re-verified. Physical-device pending: perceptible pulse strength/timing on Android, toggle absence on a genuinely non-vibrate browser, real-finger commit/cancel feel, OS-level vibration scheduling vs the 250ms window. |
| 7 — A11y hardening | not started |
| 8 — QA & migration | not started |
